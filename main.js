// I included the codecademy provided files due to the below.
// I am not sure if I need it or not.
// I'll remove the files if i don't use them...
const prompt = require('prompt-sync')({sigint: true});

const hat = '^';
const hole = 'O';
const fieldCharacter = '░';
const pathCharacter = '*';
